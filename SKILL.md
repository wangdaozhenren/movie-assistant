---
name: movie-assistant
slug: movie-assistant
displayName: 电影助手
description: 电影票比价助手——同影片同影厅同时段，买最便宜的票。当用户想比电影票价格、电影票哪里便宜、影院票价对比、电影票价、选座购票、movie ticket comparison 时使用。覆盖：电影票优惠频道（全国影院低至约 85 折）、猫眼、淘票票。影片/影院/场次搜索，票价跨平台对比，座位分区价格提示（黄金区/普通区），退改签规则同步展示。需城市与影院/场次信息；网页端需登录时移交用户处理。只做比价，不代下单、不代付款。
summary: "电影票比价助手——同影片同影厅同时段，买最便宜的票。当用户想比电影票价格、电影票哪里便宜、影院票价对比、电影票价、选座购…"
version: 1.0.0
license: MIT
tags: 电影票比价,电影票,影院,选座,票价,猫眼,淘票票,85折,退改签,黄金座,movie-ticket,电影助手
---

# 电影助手

同一部影片、同一家影院、同一场次，比**真实票价**。

## 工作流

1. **解析需求**：提取 `城市`、`影片名`、可选 `影院/商圈`、`日期`、`场次时段`、`人数`。缺城市或影片时先问一次。

2. **渠道路由**：优先读本机 `data/jutuike_acts.json` 筛选电影票活动；需最新列表调 `/acts?platform=jutuike&cate=电影票`。常规对比平台：聚推客电影票（低价入口）→ 猫眼 → 淘票票（用户点名平台时以用户为准）。

3. **逐渠道采集**（读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")`）：搜索影片 → 选城市/影院 → 选场次 → 读票价。每条记录至少含：`平台`、`影院`、`场次`、`座位区`、`票价`、`服务费/手续费（如有）`、`退改规则`、`链接`。

4. **登录/验证移交**：登录墙/验证码出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

5. **口径归一（读 `references/movie-assistant-knowledge.md`）**：
   - **同影院同时段**才可比：不同影院、不同场次（IMAX/杜比/普通）价格无意义；
   - **座位分区**：黄金区/中间排与普通区/边角价格不同，分开列；
   - **真实票价**：页面显示价是否含服务费/手续费，标注口径；
   - **退改规则**：开映前可退/不可退/改签费用，同价不同规则要提示。

6. **报告输出**：Markdown 表 `平台 | 影院 | 场次 | 座位区 | 票价 | 服务费 | 退改 | 链接 | 备注`，结论给出"最低价购票路径 + 座位/退改提醒"。**每个价格必须附可点击的购票链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为内部实现（复用主技能 `data/union_config.json` 与 `scripts/link_helper.py`，对使用者明示）。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单影片 ≤2 次/日、两次页面操作间隔 ≥10 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代下单、不代付款；票价页面实读，禁止编造。

## 边界声明

- 票价随场次余票实时变动，报告标注"价格截至 <读取时点>"。
- 特殊场次（IMAX/杜比/CINITY/4DX）票价与特效费计入口径，跨厅不直接比。
- 退改规则以平台与影院页面为准，报告提示"购票前确认退改条款"。
